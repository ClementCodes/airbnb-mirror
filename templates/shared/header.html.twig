<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="background-color: rgb(12, 12, 10) !important">
	<div class="container-fluid">
		<a class="navbar-brand" style="color: rgb(243, 59, 59)" href="#">Site mirror airbnb</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('homepage') }}">Accueil

					</a>

				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('ads_index') }}">Annonces !
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
			</ul>
			<ul class="navbar-nav ml-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('homepage') }}">Mon compte !
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>

{# ici le dump permet de voir les differentes possibilites de l'app generale ici le app.flashes corresponds  au app.flashes crée dans le adController  #}
{# {{ dump(app) }} #}
{# {{ dump(app.flashes) }} #}

	{% for label , messages in app.flashes %}
		<div class="container"> <div class="alert alert-{{label}}">

			{% for message in messages %}
				<p>{{message |raw}}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
